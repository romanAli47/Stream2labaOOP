#include "Sorting.h"


Sorting::Sorting()
{
}

void Sorting::SortByMark()//—ортировка по названию университета
{
	vector <Shop>::iterator index;
	if (bs.size() != 0)
		sort(bs.begin(), bs.end()); //less<Sorting>());/*—ортировка в лексикографическом пор€дке,
	// ћетод begin возвращает значение итератора Ц указател€ на начало контейнера, метод end Ц на конец*/
}

Sorting::~Sorting()
{
}
